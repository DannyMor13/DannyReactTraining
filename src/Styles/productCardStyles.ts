//I'd add a type to these objects (SxProps)
const cardStyle = {
  width: 300,
  height: 345,
  textAlign: "center",
  position: "relative",
};

const cardBtnsStyle = {
  display: "flex",
  justifyContent: "space-between",
  position: "absolute",
  bottom: 0,
  left: 0,
  right: 0,
};

export { cardStyle, cardBtnsStyle };
